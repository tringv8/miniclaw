# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# Telegram (nếu dùng)
TELEGRAM_BOT_TOKEN=

# Gmail / Google Calendar (nếu dùng OAuth)
# Không điền ở đây — setup qua lệnh: docker compose run --rm miniclaw-cli connect gmail
